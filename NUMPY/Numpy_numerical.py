# Numpy numerical operations

import numpy as np
radar_readings=np.array([0.2,0.5,9,5,4.3,5.8,2,4.0,6,8,3.4]) #in meters

print(np.sqrt(radar_readings))
print(np.round(radar_readings))
print(np.ceil(radar_readings))
print(np.floor(radar_readings))
print(np.pi)
print(np.exp(radar_readings))